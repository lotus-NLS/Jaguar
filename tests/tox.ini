[tox]
envlist = pkg, req
toxworkdir=/tmp/.tox/{env:TOX_ENVNAME}

[testenv]
passenv = SSH_AUTH_SOCK
commands = python -m unittest discover -s . -p "t_*.py"

[testenv:pkg]
deps = -e ..

[testenv:req]
deps = -r../requirements.txt
